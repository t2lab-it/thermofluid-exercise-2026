---
title: "テストと数値検証"
---
::: {.course-position}
参照・テストと数値検証
:::

このページは，各課題でテストの意味や数値計算結果の妥当性を確認するときに参照するガイドです．
テストの書き方と数値結果の読み方をまとめます．
実行・提出の手順は[学生向け課題ワークフロー](workflow.qmd#tests)を参照してください．

## 実行するコマンド

学生リポジトリのルートディレクトリで実行してください．
初回の環境復元は[環境診断（F00）](../assignments/F00.qmd)で行います．

```console
$ julia --project=. -e 'using Pkg; Pkg.test()'
（実装・自作テスト完成後の例）
Test Summary: | Pass  Total  Time
（テスト名・成功件数・実行時間を表示）
```

`Pkg.test()`とCIは現在課題と完了済み課題のテスト，および公式生成物のサイズ上限を確認します．

## 三種類の確認

1. **具体例**: 手計算できる少数の入力で式と添字を確認する．
2. **性質**: 定数保存，有界性，入力不変，最終時刻など複数入力に共通する意味を確認する．
3. **失敗条件**: `NaN`，空配列，不正なCFLなど，契約外の入力が明示的に拒否されることを確認する．

図，単位，符号，最小・最大，CFLと併せて判断します．

### 浮動小数点のテスト

偏差は平均からのずれです．

$$
T_i'=T_i-\bar{T}
$$

定義より，偏差配列の和は厳密計算なら

$$
\sum_i T_i'=0
$$

です．
ただし，この関係を== 0による完全一致としてテストすると，丸め誤差のため安定しません．
浮動小数点の演算では丸め誤差が生じるためです．
このようなときは，「厳密に0なこと」ではなく「十分0に近いこと」を検査します．
この性質は，特定の入力例だけでなく多くの入力に共通するテストになります．

## 課題内のテスト {#exercise-tests}

| 提出単位 | 配布されるテスト | 自分で書くもの |
|---|---|---|
| F01，F02，F03-F04 | 実行可能な必須テスト | 自作TODO一つ |
| N01 | 五つの必須TODOと一つの自作TODO | 各TODOの入力，Juliaコード，期待値または判定基準 |
| N02 | 必須TODO4群と指定テーマの自作TODO2つ | 周期総和保存と循環移動との整合を両方実装し，入力・期待値・許容誤差の根拠を書く |

配布済み必須テストを保ち，指定されたTODOの`@test false`を自分のテストへ置き換えてください．
入力・期待値・判定基準を選び，許容誤差を使う場合はその根拠も説明します．

学習ログには次を記録してください．

- 配布済み必須テストの意味
- 自分で書いたテストの入力と期待値の根拠
- 各テストが保証することと保証しないこと

一課題を調べる場合は，リポジトリのルートから`julia --project=. exercises/F01_first_pull_request/tests.jl`のように直接実行できます．

## 1次元線形移流課題の公式出力

### 公開基準値と比べる

N01の出力ファイル，確認項目，基準値と比較図は，[N01課題の「公開基準値と比べる」](../assignments/N01.qmd#reference-results)を参照してください．

## 失敗時の切り分け

最初の失敗またはエラーから読みます．
次を秘密情報を除いて記録してください．

- 実行した完全なコマンド
- JuliaとOS
- 最初に失敗したテストセット，期待値，実際値
- 直前に変更したファイルの`git diff`

AIをデバッグ補助に使う場合も，解釈と採否は自分で決め，学習ログへ残します．

N02の条件・図・総和と波形の判定量は，[N02課題の公開基準値](../assignments/N02.qmd#reference-results)を参照してください．
