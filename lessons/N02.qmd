---
title: "保存形Burgers方程式と非線形移流"
lesson-id: N02
understanding-check-points:
  - "線形移流方程式の一定速度と，Burgers方程式の値に依存する速度の違い"
  - "保存形の流束後退差分と非保存形の離散化の違い"
  - "固定と周期の格子・境界，旧配列だけを読む更新，バッファ交換の順序"
  - "実効最大CFL，有界性，周期総和，循環移動との整合で検出できる誤りと限界"
  - "N01との波形比較，自作2つの入力と許容誤差の根拠"
---
::: {.course-position}
第6回・授業 · 課題ID: N02
:::

## この回の到達点

[N01](N01.qmd)の線形移流方程式（移流速度が一定）を発展させた保存形Burgers方程式（非線形移流方程式，移流速度がその点の分布の値に依存する）を実装します．

## 保存形Burgers方程式

今回は以下の問題を数値的に解きます．

$$
\frac{\partial u}{\partial t}+\frac{\partial f(u)}{\partial x}=0,
\qquad f(u)=\frac{u^2}{2}.
$$

解 $u$ が滑らかな部分では，連鎖律と $f'(u)=u$ により $u_t+u u_x=0$ と書けます．
すなわち，分布の値 $u$ 自体がその場所の移流速度になります．
N01で取り組んだ線形移流方程式では， $u$ がどのような値であっても同じ速度 $c$ で運ばれましたが，ここでは $u$ が大きいほど速く進みます．

## 解く問題と標準条件 {#problem-setup}

領域は $0\le x\le2$ とし，初期条件を

$$
u(x,0)=\begin{cases}
2 & (0.5\le x\le1),\\
1 & (\text{それ以外})
\end{cases}
$$

とします．次の二つの境界条件で計算します．

- **固定境界**：左端は $u(0,t)=1$，右端は勾配0とします．内部点を更新した後，左端に1，右端に左隣の値を設定します．
- **周期境界**：領域の左右をつなぎ，全点を更新します．先頭の左隣は末尾です．

| 条件 | 格子 | 点数 $n_x$ | 格子間隔 |
|---|---|---:|---|
| 固定 | $[0,2]$ | 81 | $2/(n_x-1)$ |
| 周期 | $[0,2)$ | 80 | $2/n_x$ |

どちらも格子間隔は0.025で，`nx`は各境界における格子点数です．
周期格子では同じ物理点を二重に保持しません．周期境界で`left`を取得するための関数が`periodic_left_index(i,n)`です．
標準条件は指定CFL 0.5，最終時刻 $t=0.5$ とします．以下の方法で時間刻みを決めると，`dt=0.00625`で80ステップの計算になります．

## 流束の差分

格子点値 $u_i^n$ に対して流束を計算し，後退差分と陽Euler法を組み合わせます．

$$
u_i^{n+1}=u_i^n-\frac{\Delta t}{\Delta x}
\left[f(u_i^n)-f(u_{\mathrm{left}(i)}^n)\right].
$$

| 数式 | 配列・関数 | 意味 |
|---|---|---|
| $u_i^n$ | `u_old[i]` | 更新前の現在点 |
| $u_{\mathrm{left}(i)}^n$ | `u_old[left]` | 更新前の左隣 |
| $f(u)$ | `burgers_flux(u)` | 格子点値の流束 |
| $\Delta t/\Delta x$ | `dt / dx` | 流束差に掛ける係数 |
| $u_i^{n+1}$ | `u_new[i]` | 更新先 |

$f(u_i)-f(u_{i-1})$ を $u_i(u_i-u_{i-1})$ に置き換えてはいけません．
連続式が滑らかな場で等価でも，この二つの離散化は異なります．
例えば現在値2，左隣1，`dt/dx=1/4`では，保存形の次の値は1.625，非保存形では1.5になります．

標準条件では $u$ は非負で，速度も非負なので左側が風上です．

## 周期境界での総和保存

周期境界では，すべての格子点で差分を足し合わせると

$$
\begin{gathered}
\sum_i (u_i^{n+1}- u_i^n)
=-\frac{\Delta t}{\Delta x}\sum_i
\left[f(u_i^n)-f(u_{\mathrm{left}(i)}^n)\right]
=0
\end{gathered}
$$

となります．各点の流束が一度ずつ足され，引かれるためです．
この性質が保存形を使う利点です．
ただし，実際の数値計算では浮動小数点の丸め誤差を許容する必要があります．
なお，固定境界では領域の端から出入りがあるため，同じ総和保存は要求できません．

## 更新の順序とCFL条件

`run.jl`を，流束 → 周期添字 → 1ステップ → 境界 → `simulate` → `main`の順に読んでください．
`nonlinear_upwind_step!`では旧配列`u_old`を読んで新配列`u_new`へ書き込み，境界条件を適用してから新旧バッファを交換します．
その場で旧配列を書き換えられないことに注意してください．

初期条件の最大速度 $a_0=\max_i|u_i^0|=2$ を使い，時間刻みを

$$
\Delta t_{\mathrm{nominal}}=\mathrm{CFL}_{\mathrm{requested}}\frac{\Delta x}{a_0},
\quad N=\left\lceil\frac{t_{\mathrm{final}}}{\Delta t_{\mathrm{nominal}}}\right\rceil,
\quad \Delta t=\frac{t_{\mathrm{final}}}{N}
$$

とします．
非負の場では $\max_i|u_i|\Delta t/\Delta x\leq1$ が安定性の条件となります．各ステップの値から実効CFLを計算します．
`max_cfl`は初期・最終状態を含む全状態で計測した最大値であり，指定CFLそのものではありません．
指定CFLを1より大きくしても計算は続行します．例えば指定CFL 2，最終時刻0.1で，標準条件と解の振る舞いを比較してみてください．
不安定化して負値が生じても同じ後退差分を続けるため，これは安定条件を外した計算の観察です．負速度に応じて風上方向を切り替える手法ではありません．
発散して値が非有限になった場合は停止します．その場合は観察する最終時刻を短くしてください．

## N01との比較とテスト

今回解く保存形Burgers方程式の解のふるまいをN01課題と比較すると，移流速度が $u$ の値ごとに異なる特徴がどのように現れるでしょうか？

周期系には特別な始点がありません．
すなわち，配列の要素を循環させても同じ結果になります．
周期1ステップを $S$，循環移動を `circshift` と書くと，確かめたい関係は

$$
S(\operatorname{circshift}(u,k))\approx\operatorname{circshift}(S(u),k)
$$

です．これにより，とくに配列の端の操作を間違えていないかを調べられます．
どのような初期条件を使うとバグが見つけやすいかも考えてみてください．

ここまで読んだら[N02課題](../assignments/N02.qmd)へ進み，実装に取り組んでください．

{{< include /lessons/_understanding-check.qmd >}}
