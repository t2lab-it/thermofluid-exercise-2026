---
title: "ベクトル解析の公式の数値的検証"
---
::: {.course-position}
第4回・課題 2/2 · 課題ID: F03
:::

## 目的

ベクトル解析の三つの公式を手計算で証明し、解析式をForwardDiffの自動微分で独立に確認してから、同じ公式を中心差分で数値的に検証します。厳密な恒等式、連続微分の参照値、離散近似、Juliaコード、テスト結果を一つの説明としてつなげる課題です。

前提は[配列・関数・テスト課題](F02.qmd)（課題ID: F02）のmergeです。branch、PR、Actions、diff、セルフレビューは[共通課題ワークフロー](../guides/workflow.qmd)に従います。

## このページの進め方

1. 授業ページで微分演算子・添字記法・中心差分を確認する。
2. 三つの公式を手計算し、途中式を学習ログへ転記する。
3. 提供された`automatic_reference`を実行する。
4. 解析式と自動微分の誤差を記録する。
5. 学生リポジトリの`run.jl`にある中心差分の四つの未実装箇所だけを完成させる。
6. `n=9`と`n=17`の格子収束を調べ、自動微分と有限差分の違いを説明する。

## 課題リポジトリ内の場所

- 編集するコード: `exercises/F03_vector_calculus/run.jl`
- 参照値の提供テスト: `test/workflow/f03_reference_test.jl`
- 提供テスト: `test/provided/F03.jl`
- 自分で追加するテスト: `test/student/F03.jl`
- 学習ログの雛形: `learning_logs/templates/F03.md`
- 提出する学習ログ: `learning_logs/F03.md`

## 課題を開始する

前の課題をmergeして更新したcleanな `main` から開始します。

```bash
git status --short
git branch --show-current
julia --project=. scripts/course.jl start F03
```

成功すると `exercise/F03-vector-calculus` へ移ります。

## 提出1: 三つの公式の手計算証明

次の三式を定義から手計算で証明してください。完成式だけでなく、成分表示または添字表示の途中式、使った性質、仮定を `learning_logs/F03.md` に記します。

$$
\nabla\times(\nabla\phi)=\boldsymbol{0}
$$

$$
\nabla\cdot(\nabla\times\boldsymbol{A})=0
$$

$$
\nabla\cdot(\nabla\phi)=\nabla^2\phi
$$

### 証明A: 勾配の回転

1. 回転の定義 $(\nabla\times\boldsymbol{B})_i=\varepsilon_{ijk}\partial_jB_k$ を書く。
2. $B_k=\partial_k\phi$ を代入する。
3. $\phi$ が2回連続微分可能であることから混合偏微分を交換する。
4. Levi-Civita記号の反対称性と二階偏微分の対称性から各成分の結論を書く。

### 証明B: 回転の発散

1. 発散と回転の定義を添字表示で順に代入する。
2. どの二つの添字について対称性と反対称性を比べるか明示する。
3. $A_k$ の混合偏微分を交換できる滑らかさを仮定として書く。
4. 縮約した総和の結論を書く。

### 証明C: 勾配の発散

1. $\nabla\phi$ の各成分を書く。
2. そのベクトルへ発散の定義を適用する。
3. 総和を $x,y,z$ 成分へ展開する。
4. Laplacianの定義と一致することを示す。

写真だけの提出にはせず、添字を検索できるテキストまたは数式として学習ログに残してください。手書き画像は補助的に添付できます。

## 提出2: 数値検証

### 使用する場

領域 $[-1,1]^3$ で

$$
\phi(x,y,z)=\sin x\cos y\,e^z,
\qquad
\boldsymbol{A}(x,y,z)=\left(\sin y\,e^z,\sin z\,e^x,\sin x\,e^y\right)
$$

を使います。解析的な $\nabla\phi$、$\nabla\times\boldsymbol{A}$、$\nabla^2\phi$ はスターターコードに用意されています。代表点1点は手計算でも確認してください。

### 提供された自動微分の参照値

教材提供の`automatic_reference(point)`は、`ForwardDiff.gradient`で$\nabla\phi$、`ForwardDiff.jacobian`で得たJacobianの成分から$\nabla\times\boldsymbol{A}$、`ForwardDiff.hessian`と`LinearAlgebra.tr`で$\nabla^2\phi$を計算します。

この関数は**提供済み・非export・学生TODOではありません**。学生が変更する四つの関数から独立した参照値として、そのまま実行します。代表点で解析式との誤差を記録し、成分の順序や符号を確認してください。

ForwardDiffの自動微分は、連続な関数を計算規則に沿って微分し、有限の刻み幅を選びません。一方、この後に実装する中心差分は有限の$h$を使う離散近似です。自動微分との一致だけで中心差分を完成とせず、格子収束も別に確認します。

| 層 | 実施すること | 学習ログの証拠 |
|---|---|---|
| 手計算 | 三つの恒等式を定義から証明する | 仮定、定義、途中式 |
| ForwardDiff | 連続微分の参照値を得る | 解析式との誤差 |
| 中心差分 | 四つのTODOを実装する | 格子収束と誤差比 |

### 実装する四つの関数

`exercises/F03_vector_calculus/run.jl` の `TODO` は次の四か所です。

- `centered_partial(f, point, direction, h)`: 指定方向の一階偏微分を中心差分で返す。
- `curl_gradient_residual(point, h)`: $\nabla\times(\nabla\phi)$ の3成分の残差を返す。
- `divergence_curl_residual(point, h)`: $\nabla\cdot(\nabla\times\boldsymbol{A})$ の残差を返す。
- `laplacian_identity_residual(point, h)`: $\nabla\cdot(\nabla\phi)-\nabla^2\phi$ の残差を返す。

`verify_identities(n)` は提供済みです。$[-1,1]^3$ の格子上で三種類の最大絶対残差を集計します。関数名、引数、戻り値の形は変更しません。

中心差分は

$$
D_i^{(h)}f(\boldsymbol{x})
=\frac{f(\boldsymbol{x}+h\boldsymbol{e}_i)-f(\boldsymbol{x}-h\boldsymbol{e}_i)}{2h}
$$

です。`direction` は1、2、3、`h` は正の有限値、評価点は有限な3成分とします。無効な入力を拒否し、入力の `point` を変更しません。

### 代表点で三層を確認

$\boldsymbol{x}=(0.2,-0.3,0.4)$で手計算した解析式と`automatic_reference`を比較し、gradient、curl、Laplacianそれぞれの誤差を記録します。続いて$h=0.1$で中心差分による三つの残差を実行前に概算します。符号、成分数、0に近い大きさを確認し、三層の証拠を学習ログで区別してください。

### 格子収束

`verify_identities(n=9)` と `verify_identities(n=17)` を実行し、各公式について

$$
\text{ratio}=\frac{E_{n=9}}{E_{n=17}}
$$

を求めます。中心差分は二次精度なので、各比が3.0以上4.8以下に入ることをテストします。三つの誤差値と三つの比を学習ログへ記録してください。

解析的な中間場へ中心差分を適用するのは、同じ離散演算子を合成したときの機械的な相殺を避け、差分近似の誤差を観測するためです。この理由も説明してください。

## 自分で追加するテスト

`test/student/F03.jl` に少なくとも次を追加します。

- 既知の関数で `centered_partial` の微分値を確かめる。
- 不正な `direction`、非正・非有限の `h`、不正な評価点を拒否する。
- 代表点で三つの残差の形と大きさを確かめる。
- `n=9` と `n=17` で全誤差が減り、誤差比が3.0以上4.8以下になることを確かめる。
- 実行後も `results/F03/` が作られないことを確かめる。

期待値は実装関数自身から作らず、手計算または解析式から独立に用意します。

## 実行とテスト

```bash
julia --project=. exercises/F03_vector_calculus/run.jl
julia --project=. test/workflow/f03_reference_test.jl
julia --project=. test/provided/F03.jl
julia --project=. -e 'using Pkg; Pkg.test()'
```

この課題に公式生成物はありません。`results/F03/` は作りません。提出物はコード、テスト、学習ログです。

## 学習ログ

`learning_logs/F03.md` に次を記録します。

- 証明A、B、Cの仮定と途中式。
- 代表点の解析式とForwardDiffによる自動微分の値、および解析式との誤差。
- 代表点の手計算、自動微分、中心差分による数値残差の比較。
- `n=9`、`n=17` の三つの誤差と誤差比。
- 連続な公式が厳密でも数値残差が0にならない理由。
- 自動微分と有限差分の違い。
- 離散演算子の機械的相殺を避けた理由。
- 追加した自分のテストと、それが防ぐ誤り。

## AI利用の境界

AIには用語の説明、エラーメッセージの解説、別の練習式について質問できます。三つの完成証明、四つのTODOの完成実装、完成テストを生成させて提出してはいけません。利用した場合は質問内容と自分で検算した箇所を学習ログへ書きます。

## 次へ進む条件

- 三つの手計算証明で使う定義、仮定、対称性を説明できる。
- 提供された非exportの`automatic_reference`と四つの学生TODOの役割を区別できる。
- ForwardDiffの自動微分と中心差分の違いを説明できる。
- 代表点の比較と格子収束で何を検証するか説明できる。

## 完了条件

- 三つの公式について、仮定・定義・途中式を含む手計算証明を学習ログへ提出した。
- 提供された`automatic_reference`を実行し、ForwardDiffの参照値と解析式との誤差を記録した。
- 中心差分の四つの固定APIを実装し、入力検査と非破壊契約を守った。
- 代表点で手計算・自動微分・中心差分の証拠を比較した。
- 中心差分の`n=9`と`n=17`で格子収束を確認し、全誤差が減って三つの比が3.0以上4.8以下になった。
- 提供テストと自分のテストが通り、公式生成物がないことを確認した。
- branch、PR、Actions、diff、セルフレビューを完了した。
